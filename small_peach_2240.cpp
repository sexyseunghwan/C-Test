#include <iostream>

using namespace std;

int N,K;
int p_arr[3][1001][32];
int input_arr[1001];
int peach_cnt = 0;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    
    cin >> N >> K;

    for (int i = 0; i < N; i++) {
        cin >> input_arr[i];
    }

    //for (int i = 1; i <= )
    
    

    return 0;
}